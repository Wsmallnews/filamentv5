## 商城改造计划：

1.产品状态 status 目前是否合理， up hidden，down drag
2.order pipe 中把老的错误的字段都更正一下
    a.说明之前 attribute 是 对应的 attribute 表，目前这个功能暂时搁置
3.地址组件应该放到那个扩展包中，用户的收货地址，后续可能还会有 userInvoice 等
4.创建商品分类 与 商品关联表 ，节省一个迁移文件，这个表按照 addons\cms\database\migrations\create_sn_posts_table.php.stub 来建，你可以手动把这个表给我创建了，或者是改好新的迁移后重新执行 product 迁移
5.目前这几个扩展包的配置文件可以暂时不发布，等开发的差不多再说不迟，另外 不管是数据迁移还是配置文件，都是扩展包中的为基准，扩展包中一定是最全的配置
6.多租户商城是我的实现目标，扩展包要支持，不过目前环境确实是 单租户环境
7.关于 product 扩展包中 Filament v3 旧死代码（src/Resources/
    a.addons\product\src\Resources\AttributeRepositoryResource.php 这个可以暂时搁置，后续会用做参考，但是目前先不做，你可以先留着不删
    b.UnitRepositoryResource  我需要现在实现，我有几个场景如下
        i.对于单位 unit 来说后续是否会有共用的场景，还是说只有 产品需要
        ii.对于 unit 来说是个非常小的资源，他可能不需要一个单独的菜单，只需要在调用处可以方便的选择 (我自己的理解，你可以补充)，那么是否我可以不创建资源，然后在调用出 商品表单中选择，如果没有，可以顺手创建一个，关于存储，我也希望直接在产品表存选中的单位的 label, 而不是 单位表的 id,这样也不需要关联查询；另外一种方式，你可以帮我分析下 filament 的  Nested resources - Filament 和  Singular resources - Filament 哪种方式更适合 实现 unit

## 关于货币：
1.所有 货币字段都用 unsignedBigInteger，你把现有的不是这个类型的货币字段都给改一下
2.是否需要每个表都存 币种
3.MoneyCast  目前是采用的 cknow/money，你帮我分析下目前现有的 github 上 关于货币处理的 php的 或者 laravel 的 扩展包 那个更好一些， 我用这个是否是最优的
4.系统中现有的关于货币的写法你都可以分析或者重构，之前的有不合理的地方需要优化，我会告诉你
5.现有系统货币处理不合理的需要优化的地方
    a.现有的字段货币都是 cast = moneyCast 的
    b.我有一个场景需要实现：订单表中我有很多字段是 json ，比如 amount_fields，discount_fields 等，它里面会有金额字段；为什么会有这种设计呢：因为我要把 order 打造成 通用订单模块，比如商城，比如 多门店，比如用户钱包充值等， 这里面会存 订单的所有费用字段明细，比如商品金额，运费，等，但这并不是通用的， 比如用户钱包充值根本没有这些字段，所以我会把这些费用都存在 json 字段中， 再加一个直接字段 存储费用总和；现在就面临一个问题，json 中的字段 应该存 带小数的，还是 像字段一样存证书，或者是每个金额字段都是 json, 包含了金额的信息 
    c.还有最重要的 json 中的 金额应该如何 进行计算
    d.因为现有 货币问题没有全部解决，这里只能提出问题，需要你帮我补全

## 关于 pay 表，我需要做一个通用的支付扩展包，可以供其他需要支付的扩展包来调用，比如shop 模块中引入的 order 模块 下单时支付处理
1.支付有如下场景
    a.国内
        i.原生微信支付宝银联支付，国内有优秀的 支付扩展包 yansongda/pay
            1.国内的支付都是多端的， 比如微信：公众号支付，小程序支付，h5 支付，app 支付；支付宝：h5 支付，app 支付，支付宝小程序支付等
            2.国内支付可能会有一些特殊的设置，比如微信支付，可能会依赖 openid 等，openid 涉及业务逻辑，他应该作为参数传入 pay 包，而不是在 pay 包中处理
            3.所以国内支付，可能需要确认， 支付的平台，支付的方式，支付额外参数等
        ii.支付平台集成， 比如 汇付天下，随行付等
            1.此类平台都集成的有 微信支付宝，银联等，可能和原生逻辑有所不同，你可以先了解他与原生之间的区别，然后进行抽象，给我最终抽象接口
    b.国外的 stripe  paddle 等，我看 laravel 有 cashier 扩展包
        i.你帮我分析下国外主要用的支付方式，我不清楚是否合理
2.我需要扩展包实现的场景如下
    a.所有需要支付的扩展都可以使用 pay 扩展包
    b.需要统一的接口和调用方式
    c.支持分批支付，目前是订单是否支付是通过计算此订单的所有支付金额是否大于应支付金额，这种模式可以兼容后续可能的 定金模式
    d.支持 退款
3.我之前是在 order model 上添加了接口，然后  Wsmallnews\Order\Contracts\PayableInterface ,原本这个接口应该是在 pay包，但上次是先改造的 order， 你给他迁移了
4.pay 表之前有很多逻辑，但是不能满足我上述说的场景，需要你重构